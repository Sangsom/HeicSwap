//
//  Theme.swift
//  HeicSwap
//
//  Created by Rinalds Domanovs on 23/02/2026.
//

import Foundation

/// Root namespace for the Warm Darkroom design system.
///
/// Tokens live in nested types — `Theme.Colors`, `Theme.Typography`,
/// `Theme.Spacing`, `Theme.Radius`, `Theme.Gradients` — and are the single
/// source of truth for the app's look. Feature code references these tokens
/// and never hardcodes colors, fonts, spacing, or radii.
enum Theme {}
